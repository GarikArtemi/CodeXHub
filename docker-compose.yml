version: "3"

services:
  api:
    build: ./apps/api

  terminal:
    build: ./services/terminal

  file:
    build: ./services/file

  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./infra/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./apps/web:/usr/share/nginx/html
