server {
  listen 80;

  location /api {
    proxy_pass http://api:4000;
  }

  location /ws {
    proxy_pass http://terminal:5000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }

  location / {
    root /usr/share/nginx/html;
  }
}
